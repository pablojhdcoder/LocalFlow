# LocalFlow frontend configuration (optional)
# Copy to .env.local for local overrides. Rebuild or restart dev server after changes.

# Set only when the UI is served from a different origin than the API.
# Leave unset in local dev — Vite proxies /api, /audio, and /thumbnails to :3000.
# VITE_API_BASE_URL=http://localhost:3000
